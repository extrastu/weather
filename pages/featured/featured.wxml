<view class="featured">
  <view class="{{item.colorClass}}" style="background:{{item.colorClass}}" id="topic" wx:for="{{featured}}" data-list="{{item}}" wx:key="index" bindtap='previewImg'>
    {{item.types}}
  </view>
</view>